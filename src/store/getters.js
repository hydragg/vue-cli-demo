// export const getCount = state => state.count 
// export const getTodos = state => state.todos.filter(todo => !todo.done) 
// export const getDone = state => state.todos.filter(todo => todo.done) 

export const getAllState = state => state